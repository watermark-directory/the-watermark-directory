# PJM wholesale-market reference (RTO / LMP / RPM capacity / queue) - PJM Interconnection

Committed PJM wholesale-market context for the campus load: the zonal **LMP** (the
energy price signal), the **capacity market** (RPM Base Residual Auction clearing
price -> the capacity obligation a large new load implies), and the **large-load
interconnection queue** the campus is sized against. The "RTO / wholesale market"
layer of the grid & regulatory stack (issue #96, epic #93) - it sits above the
serving-utility / BA identification (#94) and the EIA-930 interchange layer (#95).
Regenerated by `bosc pjm`; the derived scenario reads `pjm-market.yaml` (no live pull).

## Files

| File | What | Source |
|---|---|---|
| `pjm-market.yaml` | The campus's PJM zone (**AEP**), the AEP-zone average annual **LMP** ($/MWh), the **RPM** Base Residual Auction RTO-wide clearing price ($/MW-day, with the prior year for context), and the reported **large-load / data-center interconnection queue** (GW, order-of-magnitude) - each a provenance-tagged figure. Regenerated by `bosc pjm`. | **PJM Data Miner 2** (`da_hrl_lmps`, AEP zone) for LMP; **PJM RPM Base Residual Auction** reports for the capacity clearing price; **PJM interconnection-queue / large-load** reporting for the queue. `source: reference` (transcribed; verify). |

## The campus's PJM marginal-price / capacity footprint

`bosc.grid.market.derive_pjm_market_scenario` links the facility's first-class total
**`facility_draw`** (`bosc.facility.power`, issue #87 - IT load x PUE) to these figures:

- **Annual consumption** = facility draw x 8760 h x load factor (~0.9, assumption).
- **Annual energy cost** = consumption (MWh/yr) x zonal LMP ($/MWh) -> ~$M/yr (derived).
- **Annual capacity cost** = facility draw (MW) x RPM clearing ($/MW-day) x 365
  -> ~$M/yr (derived). (~348 MW x $269.92/MW-day x 365 ~= **$34M/yr** - the striking
  capacity footprint a single large new load implies at the 2025/2026 clearing price.)
- **Campus vs the queue** = campus MW as a fraction of the large-load queue (GW).

The capacity footprint and the queue framing are the headline. They are a **screening
"bracket, don't overclaim" view, not a settlement, dispatch, or RPM-bidding model** -
the campus's real costs are settled hourly / by delivery year against its actual
contracts, not these annualized averages.

## Known gaps & caveats

- **No live PJM connector - figures are TRANSCRIBED.** PJM Data Miner 2 needs no API
  key but is not reachable from the build/test environment, so the published values are
  transcribed into `pjm-market.yaml` at `source: reference`, `confidence: medium`, each
  flagged *verify*. **Regenerate / verify via PJM Data Miner 2** (LMP), the **PJM RPM
  Base Residual Auction reports** (capacity clearing), and **PJM interconnection-queue
  reporting** (large-load interest). Never fabricate - prefer omission over invention.
- **LMP varies by NODE and HOUR** (LMP = energy + congestion + losses). The committed
  figure is the **AEP-zone annual average** (~$35/MWh) - a single point, not the
  campus's bus-specific or peak-hour price; congestion is a real component.
- **The RPM clearing price is an RTO-wide CLEARING price, not the campus's contracted
  capacity rate.** The committed 2025/2026 Base Residual Auction figure (~$269.92/MW-day)
  is a major **spike** from the prior 2024/2025 year (~$28.92/MW-day, recorded for
  context) and may not persist; the campus's actual obligation depends on its
  coincident-peak contribution, not nameplate MW.
- **The large-load / interconnection-queue figure is ORDER-OF-MAGNITUDE** (tens of GW)
  and not all queued load is built; the committed ~50 GW value carries an explicit
  "order-of-magnitude, verify" citation and the campus share is illustrative.
- **The campus buys at negotiated wholesale rates, not these public averages.** This is
  the *market-context reference*, not the campus's own bill, and **not a facility
  disclosure** - the only facility-specific input is the first-class `facility_draw`.
