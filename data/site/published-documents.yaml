# Public publish policy for source documents — PUBLISHABLE-BY-DEFAULT WITHIN REVIEWED
# SCOPE, WITHHOLD DECLARATIVELY (epic #274 / #280).
#
# The object store serves the WHOLE corpus in dev/preview, but the PUBLIC site exposes
# only what this file clears. The model is two-sided:
#
#   * CLEARED SCOPE (below) — the fail-safe eligibility gate. A rel is public only if it
#     falls inside a cleared boundary; a file in NO cleared boundary is never public, so an
#     unreviewed file stays private by default. INSIDE a cleared boundary every file
#     publishes — you no longer restate each one. Curated exhibits (exhibits.yaml) are
#     auto-included (they are already published downloads).
#   * WITHHOLD (bottom) — the declarative opt-out. A withhold rule carves a specific rel,
#     subtree, or glob back out of a cleared boundary. Withhold WINS over everything,
#     including the auto-included exhibits.
#
# A boundary is cleared here ONLY after the C2 redaction/PII pre-publication pass (#281);
# a withhold rule then carves individual files back out. See published-documents.README.md.
# Three rule kinds (all optional; combine freely):
collections:      # whole data/documents/<slug> trees, e.g.  - aedg
  # The whole Ohio EPA collection. ⚠️ THE SCOPE SENTENCE BELOW IS THE 2026-07-10 BASELINE AND IS
  # KEPT AS WRITTEN, because it is what the standing clearance was granted against — but it is no
  # longer the collection's size. As of 2026-08-17 the tree holds **114 PDFs across the Lima top
  # level (17) plus TEN site subdirectories** (bowling-green, defiance, findlay, ottawa, sidney,
  # troy-piqua, van-wert, west-union, wilmington, urbana). It grew by the standing auto-publish
  # rule below, which is the point: this count will keep drifting, so re-count rather than trust
  # any number written here.
  #   [2026-07-10 baseline] The whole Ohio EPA collection — 40 PDFs (permits, draft permits,
  #   fact sheets, public notices) across the Lima top level plus five sites (troy-piqua, sidney,
  #   van-wert, wilmington, urbana). These are Ohio EPA public agency records with no private PII —
  #   the same basis on which the Bistrozzi PTI (permits/bistrozzi-permits/4132514.pdf) is
  #   published. A whole-collection rule is intended: OEPA pulls for future sites are agency
  #   records and auto-publish by the same standing basis (#1370).
  #
  # ⚠️ AMENDED 2026-08-17 (#2050) — THE "NO PRIVATE PII" CLAIM ABOVE IS NO LONGER TRUE OF THIS
  # COLLECTION, and the standing auto-publish rule is what made that matter. The West Union pull
  # added 32 documents (`oepa/west-union/`, the Buck Canyon isolated-wetland and construction-
  # stormwater files), and they cleared on this rule without a per-pull decision. Three of them —
  # the NPDES Notice of Intent, its approval letter and the Kimley-Horn site map — print the home
  # address and personal telephone of DEREK HARMON, who is the applicant of record as an
  # INDIVIDUAL rather than a company.
  #
  # Published knowingly on the operator's decision, on the same footing as the Bowling Green OPSB
  # service-list home address cleared under `grid/bowling-green/*`: a permit applicant's contact
  # block is intrinsic to the instrument, Ohio EPA publishes these exact PDFs on its own portal,
  # and the documents are load-bearing (the site map is the only source for the campus's
  # DISTURBED AREA = 535.00 AC). The finding is recorded here rather than left to be discovered.
  #
  # ⚠️ FOR THE NEXT OEPA PULL: this collection now auto-publishes into a rule whose PII premise has
  # been broken once. An `oepa/<slug>/` pull that carries an individual's contact details is no
  # longer a surprise — check for one and record it here, or carve it out with a `withhold:`.
  # ⚠️ AMENDED 2026-08-22 — THE LARGEST SINGLE PULL THIS COLLECTION HAS TAKEN, and the first where
  # the PII in it belongs to people who are not party to the permit at all.
  #
  # `oepa/lima/` added **93 documents (182 MB)** — the enforcement, inspection and permit-action
  # tranche of the City of Lima WWTP's NPDES record on permit 2PE00000, 2014-2026: 12 Judicial
  # Orders, a DFFO, 3 NOVs, 4 ROV/RTC, 30 inspections, 38 permit actions, and the correspondence
  # and incident/investigation reports. This is not a permit file; it is a municipal sewage
  # plant's operating and enforcement history, a different genre from everything cleared above.
  # So it was screened rather than assumed, and the screen is recorded here because the standing
  # rule cleared it automatically.
  #
  # THE SCREEN BELOW COVERS ALL 261 DOCUMENTS THE PORTAL PULL RESOLVED, not only the 93 committed.
  # The other 168 (routine reports, monitoring/sampling, plan sets, and the 16 permit-application
  # packages that were alone ~712 MB) were fetched and screened but deliberately NOT committed —
  # the repository exceeded its Git-LFS budget, and 1.35 GB of routine operating paperwork is the
  # part of this record that earns its bytes least. Their docids are recorded in
  # data/research/oepa-portal-2pe00000-2026-08-22/manifest.yaml and each is re-fetchable, so the
  # record of what EXISTS stays complete. Screening them anyway is deliberate: if any is committed
  # later it arrives already cleared, rather than auto-publishing on this same rule unexamined.
  #
  # SCREEN (2026-08-22): 10,436 of 10,455 pages — **99.8%**. 9,076 pages read from the text layer;
  # the remaining 1,379 were image-only and were OCR'd (tesseract, via `site.passages.ocr_page_text`
  # — the same reader the retrieval index uses) rather than counted clean, which is the #2031 rule:
  # a regex over a scan returns clean because there is no text, not because there is no PII. 19
  # pages stayed blank after OCR and are the honest residue of this pass.
  #   ZERO SSNs — the single ###-##-#### match is `EPA 4104 (720-05-3910)`, a form number on a
  #     1978 form.
  #   ZERO dates of birth. ZERO personal email: all 326 addresses are institutional (86% .gov/.us —
  #     epa.ohio.gov, cityhall.lima.oh.us, limaohio.gov — the rest Barnes & Thornburg, Frost Brown
  #     Todd, Schottenstein Zox, consultants, the county). No free-mail domain appears anywhere.
  #   Street-address matches resolved to facilities, OCR variants of one address, outfall/node
  #     numbers with leading zeros (`002 Collett Street` is an OUTFALL NUMBER beside an SSO basin
  #     name), and pump-station elevations in a hydraulic model (`840 West Street` is 840 ft).
  #
  # ⚠️ THE ONE REAL FINDING — FOUR SLUDGE-APPLICATION DOCUMENTS NAME PRIVATE INDIVIDUALS.
  # All four are among the 168 NOT committed here. Recorded anyway, because they are re-fetchable
  # by docid and a later commit of them must not have to rediscover this:
  #   edoc-1301558 (25 blocks) · edoc-1301627 (21) · edoc-1301621 (14) · edoc-1301401 (2)
  # ~62 "owner/operator of the farm" blocks on Ohio EPA site-inspection request forms for
  # agricultural land receiving sewage sludge, each printing a named individual and their full
  # home/farm address (e.g. the owner of a 98-acre application site in Auglaize Township; a farm
  # owner and its operator in Elida).
  #
  # These are NOT the #2050 shape and the difference was weighed rather than glossed. Derek Harmon
  # was the APPLICANT — an individual who chose to file, whose contact block is intrinsic to his own
  # instrument. These are THIRD PARTIES: neighbours whose land received biosolids under someone
  # else's permit, incidental to it, and roughly sixty of them rather than one.
  #
  # Published knowingly on the operator's decision (2026-08-22), on the standing basis: they are
  # Ohio EPA public agency records, Ohio EPA serves these exact PDFs from its own public portal, and
  # a sludge-application site's location and consenting landowner are the substance of the record
  # rather than an incidental detail in it — a land-application program cannot be audited without
  # knowing whose land. Recorded here, in the specific, so it is a decision on the file and not a
  # discovery waiting to happen.
  #
  # ⚠️ FOR THE NEXT OEPA PULL: the warning below still stands, and this pull is now its second
  # instance. The premise has been broken twice. Treat "agency records contain no private PII" as
  # false for this collection and screen every pull — including the image-only pages.
  # ⚠️ AMENDED 2026-08-23 (#2089) — `oepa/lima/` added **12 documents (4.4 MB)**, the APP285104563
  # indirect-discharge application package on permit 2DP00130: the Bistrozzi/"Project BOSC" campus
  # applying to send domestic waste plus non-contact cooling water to the American-Bath POTW. A
  # different genre again from the two pulls above — an APPLICANT's own filing rather than an
  # agency's enforcement record — and the first in this collection to carry third-party COMMERCIAL
  # material (four vendor safety data sheets and two laboratory certificates of analysis).
  # Screened anyway, because the standing rule cleared all twelve automatically.
  #
  # SCREEN (2026-08-23): 67 of 67 pages — **100%**. 64 read from the text layer; 3 were image-thin
  # and were OCR'd (tesseract, via `site.passages.ocr_page_text`), and all 3 stayed blank because
  # they ARE blank — the trailing pages of the 12-page sampling-report variant edoc-4116226.
  # ⚠️ Three pages carry meaning that no regex could see and were READ AS RENDERED IMAGES rather
  # than counted clean, per the #2031 rule: the one-page wastewater flow schematic (4116227, 112
  # characters of text layer), the EMH&T sanitary site-plan exhibit (4116229, a drawing), and the
  # surrogate results table appended at 4116204 p9.
  #   ZERO SSNs. ZERO dates of birth. ZERO account, routing or card numbers.
  #   FOUR distinct email addresses, none personal: smithmif@google.com (the applicant's own
  #     facility manager, the contact of record on its instrument), bniemeyer@allencountyohio.com
  #     (a county public office), and two vendor SDS support desks (Univar, MilliporeSigma).
  #   35 phone matches, ALL institutional — the applicant's contact block, CHEMTREC and vendor
  #     emergency lines, Alloway's three laboratory offices, and the Allen County Sanitary
  #     Engineer's office and fax. No residential number appears.
  #   Street matches ALL resolve to facilities or business addresses: the campus (4110 N Cole St),
  #     Google CLB-1 (225 Rathmell Rd), County Sanitary Engineering (3230 N Cole St), Alloway's
  #     labs, EMH&T (5500 New Albany Road), and the four chemical manufacturers.
  #
  # SIX INDIVIDUALS ARE NAMED and all six are the #2050 shape rather than the 2026-08-22 shape:
  # each appears in a professional capacity on an instrument they are party to — Michael Smith
  # (Facility Manager, the signer) and Robert Zizzo (Site Superintendent) for the applicant, Brad
  # Niemeyer, P.E. (Allen County Sanitary Engineer, a public official), Jeremey Eisele, P.E.
  # (EMH&T, the consulting engineer of record), and Joel Phillips and Brad Eckenrode (ChemTreat
  # laboratory staff who signed the certificates). None is a third party incidental to someone
  # else's permit, and none has a home address or personal telephone printed.
  #
  # ⚠️ THE ONE ITEM THIS SCREEN COULD NOT RESOLVE, recorded unresolved rather than glossed. The
  # Alloway certificate (edoc-4116205) ships to "Chemtreat, Inc. (Westerville), Attn: Brad
  # Eckenrode, 1008 Egret Ct, Westerville, OH 43082". Egret Ct reads as a residential street, so
  # this MAY be a sales representative's home office rather than a company address; the document
  # does not say, and nothing else in the corpus settles it. Published on the operator's decision
  # with that uncertainty stated: it is the ship-to block of a commercial laboratory report filed
  # with the agency by the applicant, Ohio EPA serves this exact PDF from its own public portal,
  # and the certificate is load-bearing — it is half of the only measured water chemistry the
  # application package contains. If it later proves residential, carve it out with a `withhold:`.
  #
  # ⚠️ FOR THE NEXT OEPA PULL: the warning above stands and this is its third instance. The
  # premise "agency records contain no private PII" remains false for this collection. Note also
  # that the collection now admits VENDOR and THIRD-PARTY-LABORATORY documents that arrive as
  # attachments to an applicant's filing — screen those for the ship-to and account blocks a
  # permit form does not have.
  # review: { by: goedelsoup, date: 2026-07-10, result: publish, basis: public-epa-agency-records }
  # review: { by: goedelsoup, date: 2026-08-17, result: publish, basis: public-epa-agency-records }
  # review: { by: goedelsoup, date: 2026-08-22, result: publish, basis: public-epa-agency-records }
  # review: { by: goedelsoup, date: 2026-08-23, result: publish, basis: public-epa-agency-records }
  - oepa
  # The whole Findlay collection — 171 files (#1265): the Hancock County Commissioners' and
  # Allen Township trustees' meeting minutes, the township zoning book + map, the WARN
  # notices, the Round 11 brownfield award, the county officials' directory and the official
  # election canvass. All are Ohio public records held by the county, the township, or the
  # State, published here on the same basis as the OEPA collection above.
  #
  # C2 PII sweep run over all 166 readable files before clearing (#281). Findings: ZERO SSNs,
  # dates of birth or account numbers; 6 email addresses, every one an official .gov or a
  # township fiscal officer's posted business address; 205 distinct phone numbers, of which
  # two are the Commissioners' office lines repeated across the minutes and essentially all
  # the rest sit in ONE file (see the withhold below); and no residential addresses at all —
  # the street/road figures are road-resurfacing appropriations (TR 114, CR 140) and county
  # buildings (courthouse, BMV, Agricultural Services). Executive sessions record only the
  # fact and the statutory purpose, never content. The election canvass is precinct-level
  # aggregate with no voter-level data.
  # review: { by: goedelsoup, date: 2026-08-13, result: publish, basis: public-county-township-records }
  - findlay
  # The whole Van Wert collection — 15 files (#1267): the City of Van Wert's council record for
  # the QTS Mega Site (the three May-2026 emergency ordinances as passed plus their posted drafts,
  # the 26-06-034 private-well draft, and six meeting records — 4.27, the 5.4 public hearing, 5.11,
  # 5.27, 6.8 and 7.13), the County Commissioners' published 2026 agenda index, and the captured
  # operator project FAQ. These are the primary instruments the site's assembly and governance
  # record rests on; every one is already published by the City, the County or the operator itself.
  #
  # Pre-publication review (#281) run over all 15 before clearing. The ordinances are clean —
  # unsigned instruments, no personal data at all (all six were posted four days before the vote
  # with an unfilled "Passed this ___ day of ___", which is the #1401 finding). The county agenda
  # index and the operator FAQ are captured public web pages. Any tokens embedded in those captures
  # are evidence, not a leak to redact, per the root CLAUDE.md.
  #
  # ⚠️ WHAT THE REVIEW DID FIND, and the basis for clearing it anyway. The six MEETING RECORDS
  # embed roughly 88 pages of written public comment from private citizens, each letter carrying
  # the writer's name and a REPRODUCED HANDWRITTEN SIGNATURE. No phone numbers or email addresses
  # appear on them. The hearing minutes note that speakers stated an address aloud, but that is in
  # the audio/video, which is not in the corpus.
  #
  # ⚠️ THIS REVIEW WAS DONE BY RENDERING SAMPLE PAGES, AND THE SAMPLE MISSED ONE. These PDFs carry
  # no text layer, so a PII regex returns a false clean and pages must be looked at — but sampling
  # is not sweeping. An earlier draft of this note asserted "no residential addresses"; page 7 of
  # 5.11.26.pdf is a submitter roster carrying fifteen, and it is now WITHHELD below. Read that
  # entry before relying on this one. Anything cleared here on a sampled read carries the same
  # residual risk, and the honest statement is "no addresses were seen in the pages sampled",
  # not "there are none".
  #
  # For the signatures, this is the same posture as aedg/PRR-01-bundle.ocr.pdf above: cleared whole as
  # DISCLOSED PUBLIC RECORD by operator decision, explicitly NOT as the outcome of a passed PII
  # sweep. Written comment submitted to a legislative body in a zoning hearing is quintessentially
  # public record, the City published these exact PDFs itself, and the evidentiary weight is high —
  # the letter campaign IS a finding (21 of 88 pages repeat an earlier body verbatim; 67 distinct
  # bodies, one submitted five times; and "$200M/20yr", "200-250 jobs" and "$80,000" appear ONLY
  # here and in no instrument). That is the opposite of the Findlay withhold below, which turned on
  # a file that nothing in the record rested on. If a specific letter ever needs carving out, use a
  # withhold rule rather than un-clearing the boundary.
  # review: { by: goedelsoup, date: 2026-08-14, result: publish, basis: disclosed-public-record }
  - van-wert
  # The whole Bowling Green collection — 15 files (#1433): eleven Middleton Township trustee
  # minutes (the 2022-2026 rezoning chain plus the meeting Meta's letter was read into), the
  # captured Meta community-impact letter, two Wood County Planning Commission agendas, the Wood
  # County Commissioners' record of 2023-09-28, and the City's 2025 Drinking Water Consumer
  # Confidence Report. These are the primary instruments this site's assembly, governance and
  # fiscal record rests on; every one is already published by the township, the county, the City
  # or the operator itself.
  #
  # Pre-publication review (#281) run over ALL 15 — a sweep, not a sample, and stated that way
  # deliberately after the Van Wert miss above. Every page is accounted for: 14 files carry full
  # text layers and were regex-swept for SSNs, dates of birth, account numbers, phones, emails and
  # street addresses; the one file with no text layer (see below) and the two image-only pages in
  # the June 2026 Planning Commission agenda were RENDERED AND READ, because a regex over a scan
  # is a false clean.
  #
  # What the sweep found clean, with the reasons: the eleven township minutes carry UNSIGNED
  # signature blocks (ruled lines with typed names beneath — the same posture as Van Wert's
  # ordinances), and although each recites that "the sign-in sheet for employees and visitors is
  # attached with the approved minutes", NO sign-in sheet is attached to any of the published PDFs
  # — verified by per-page text density, which finds no image page in any of them. The drinking
  # water report's contacts are institutional (mfields@bgohio.gov, the Water Supply
  # Superintendent's office line, the EPA Safe Drinking Water Hotline, City Hall). 07192023.pdf's
  # "20921 Dunbridge Road" is a NUISANCE PROPERTY set for structure removal under R.C. 505.86(B) —
  # the subject of official action, not a private contact detail. The agendas' image pages are an
  # adjournment stub and the County Engineer's aerial parcel map of the thirteen rezone-request
  # parcels. The Meta letter is signed "The Meta Global Infrastructure Team", no individual.
  #
  # ⚠️ WHAT THE REVIEW DID FIND, and the basis for clearing it anyway.
  # commissioners/10032023.pdf has NO TEXT LAYER (a Canon copier scan, 7 pages), so it was read
  # page by page. Page 3 carries the three County Commissioners' and the Clerk of the Board's
  # REPRODUCED HANDWRITTEN SIGNATURES — public officials executing an instrument, which is the
  # ordinary form of an adopted resolution. Page 7 is an ATTENDANCE ROSTER, handwritten, headed
  # "NAME | DEPARTMENT/ADDRESS". Seven of its eight signers gave an organizational affiliation
  # (NWWSD, Econ. Dev., B&G) or a firm address. The eighth, ROSS FOUGHT, gave a RESIDENTIAL
  # ADDRESS — 21599 N. Dixie Hwy — and he is the Wood County resident recorded on pages 1-2 as the
  # member of the public who spoke AGAINST the tax abatement.
  #
  # Cleared whole as DISCLOSED PUBLIC RECORD by operator decision, explicitly NOT as the outcome of
  # a passed PII sweep — the same posture as aedg/PRR-01-bundle.ocr.pdf and the Van Wert meeting
  # records above. The reasoning is that this file is the ONLY source in the corpus for Wood County
  # Resolution 23-01249, the 75% / 15-year Community Reinvestment Area agreement moved on 50
  # full-time jobs and a $4,000,000 annual payroll with Eastwood, Otsego and Penta Career Center as
  # the school parties — the spine of this site's fiscal and governance record, and the instrument
  # that shows the abatement was granted on HALF the headcount now in circulation. A roster signed
  # at a public meeting of a public body is quintessentially public record, and the County
  # published this exact PDF itself. Withholding the file would remove the primary instrument to
  # protect a datum the County already published; the withhold mechanism operates on files, not
  # pages, so there is no narrower carve available. If that trade ever needs revisiting, add a
  # withhold rule rather than un-clearing the boundary.
  # review: { by: goedelsoup, date: 2026-08-14, result: publish, basis: disclosed-public-record }
  - bowling-green
  # The whole West Union collection — 34 files (#2047): the Adams County Regional Water District's
  # own record behind the Buck Canyon campus. Governance (the 1968 articles of its predecessor
  # company, both eras' by-laws, the 1969 minutes, the 1993-98 election/appointment run), fifteen
  # board minutes 2024-01 → 2026-07, ten budget/financial documents, and the two Ginger Ridge
  # instruments (the executed NDA and the water-main plan set). Ohio public records of a public
  # body created by court order under R.C. Ch. 6119, published on the same basis as the Findlay
  # county/township collection above.
  #
  # Pre-publication review (#281) run before clearing. Method matters here: seven of these files
  # are scans with NO text layer, so a PII regex returns a false clean on them and every page was
  # RENDERED and read instead. The 1993-98 file was swept page by page, all 13 of 13.
  #
  # ⚠️ WHAT THE SWEEP FOUND, and the basis for clearing it anyway. `1993-1998
  # Elections-Appointments.pdf` carries TWENTY-ONE private individuals' full residential street
  # addresses — candidate slates read into the annual meetings of 1993, 1994 and 1996, plus two
  # appointment resolutions that recite an appointee's home address in the operative clause
  # (Res. 97-19, Chris Baxla; Res. 98-49, Daniel Fristoe). FOUR of the twenty-one sat on this
  # board within the last three years: Howard A. "Tony" Staggs (the sitting President), Chris
  # Baxla, Gerald Gray, and Daniel Fristoe (a trustee as recently as the 2024-01-11 minutes).
  # These are 1993-98 addresses and are not asserted to be current, but three of the four are
  # sitting officials and the file is the only place in this corpus they appear.
  #
  # Cleared WHOLE as DISCLOSED PUBLIC RECORD by operator decision — explicitly NOT as the outcome
  # of a passed PII sweep, the same posture as aedg/PRR-01-bundle.ocr.pdf and the van-wert meeting
  # records above. The evidentiary weight is the reason: this file holds the single most important
  # governance document in the site's record — the members' 579-141 vote of 1993-05-13 to amend the
  # Articles and convert to a Ch. 6119 regional water district, i.e. the moment an elected board
  # became a court-appointed, self-perpetuating one — together with the first exercises of that
  # self-appointment power (Res. 97-119 and 98-90, in which the trustees being re-appointed
  # abstained on their own re-appointments). Candidacy for the board of a public water utility is a
  # public act. The alternative considered and rejected was a withhold rule with the governance
  # facts extracted; the operator elected to publish the instrument itself.
  #
  # The Ginger Ridge plan set was cleared on a different and narrower basis: its sheet G-003 master
  # table enumerates all 32 route parcels as owner NAME + auditor parcel ID, with NO street or
  # mailing addresses — published county auditor CAMA data on a public engineering drawing. One
  # construction note names a landowner ("JOHN CONDON (OWNER) TO APPROVE PATH THROUGH THE YARD").
  # Coverage note: 3 of 24 sheets were rendered and read in full plus the G-003 master table, which
  # is itself the exhaustive parcel index — so the owner inventory is complete, but this is not a
  # claim that every one of the 24 sheets was examined.
  #
  # The remaining 32 files are clean: board minutes, budgets, financial statements and by-laws,
  # whose only personal data is the names and official work contacts of District officers.
  # review: { by: goedelsoup, date: 2026-08-16, result: publish, basis: disclosed-public-record }
  - west-union
globs:            # fnmatch globs over the data/documents rel, e.g.  - 'permits/*/*.pdf'
  # Findlay's grid instruments — the AEP large-load request, the Rocky Ford 138 kV station
  # LON + public notice, the Ebersole-Fostoria construction notice, and the AEP Ohio tariff
  # book (#1265/#1464). Utility and PUCO filings, all public. A GLOB rather than the `grid`
  # collection ON PURPOSE: `data/documents/grid/` is shared across eight sites, so clearing
  # the collection would publish seven other sites' grid documents that no review has covered.
  # review: { by: goedelsoup, date: 2026-08-13, result: publish, basis: public-utility-filings }
  - 'grid/findlay/*'
  # Van Wert's grid instrument — AEP Ohio's public notice for the Van Wert–Haviland 138 kV route
  # adjustment (OPSB 26-0729-EL-BLN, the second case on the line first approved as
  # 25-0697-EL-BLN). A utility public notice, public by its own nature. A GLOB rather than the
  # `grid` collection for the same reason as Findlay's above: `data/documents/grid/` is shared
  # across eight sites, so clearing the collection would publish seven other sites' grid documents
  # that no review has covered.
  # review: { by: goedelsoup, date: 2026-08-14, result: publish, basis: public-utility-filings }
  - 'grid/van-wert/*'
  # Bowling Green's grid instruments (#1433/#1437/#1440) — the OPSB Staff Report of Investigation
  # in the Apollo Power Generation Facility case 25-0973-EL-BLN, FirstEnergy's filed Toledo Edison
  # P.U.C.O. No. 8 electric service tariff (168 pp), and the EEI "Large Load Projects and Tariffs"
  # compilation. Utility, siting-board and industry filings, all public. A GLOB rather than the
  # `grid` collection for the same reason as Findlay's and Van Wert's above: data/documents/grid/
  # is shared across eight sites, so clearing the collection would publish seven other sites' grid
  # documents that no review has covered.
  #
  # All three swept page by page (#281): full text layers throughout, no image-only page, and no
  # SSNs, dates of birth, account numbers, phone numbers or email addresses anywhere in the 210
  # pages.
  #
  # ⚠️ ONE RESIDENTIAL ADDRESS, cleared knowingly. The Staff Report's page 21 is the "OPSB Staff
  # Report Distribution List". Nine of its ten entries are state agency or legislative offices
  # (PUCO, Development, Health, Agriculture, Ohio EPA, ODNR, and the House and Senate at the
  # Statehouse). The tenth is GREGORY SLONE, OPSB Public Member, at 6123 Sugar Maple Dr.,
  # Westerville — a home address, and it appears because the Public Member is the one seat on that
  # board with no agency office to be served at. He is a serving public official, the address is on
  # the service list of a document the Ohio Power Siting Board filed and published on its own
  # docketing system, and the report is the primary instrument for this site's power story: the 350
  # MW net cap under Condition 14, Condition 15's bar on PJM interconnection, and the three
  # major-source margins. Cleared as a public agency record on that basis, with the finding
  # recorded here rather than left to be discovered.
  # review: { by: goedelsoup, date: 2026-08-14, result: publish, basis: public-utility-filings }
  - 'grid/bowling-green/*'
  # West Union's federal §404 file (#2047) — the 14 USACE Huntington District email threads in
  # LRH-2025-00457-OHR (-Elk Run), the Buck Canyon Site jurisdictional determination through the
  # NWP 39 verification. Federal agency correspondence, produced as a records production. A GLOB
  # rather than the `usace` collection for the same reason as the `grid/` globs above: this
  # collection is nested BY SITE and will hold other sites' Corps files, which no review covers.
  #
  # All 14 swept — full text layers throughout, 68 pages. No SSNs, dates of birth, account numbers
  # or residential addresses. The personal data present is the official capacity contact detail of
  # named public servants and consultants: agency and firm email addresses, office phone numbers,
  # and the Corps project manager's GOVERNMENT-ISSUED MOBILE number, which he appends to his own
  # signature block on every message. Cleared as public agency correspondence on that basis, with
  # the mobile number noted here rather than left to be discovered.
  #
  # ⚠️ ONE FILE CARRIES A DOCUMENT PASSWORD, cleared knowingly. `Email Chain 1.pdf` p.4 is the
  # consultant's transmittal of the WOTUS delineation report to the Corps, and it types a download
  # password into the message body. Cleared for three reasons, recorded here rather than left to be
  # discovered: (1) it is not our credential — it belongs to a third-party consultant, protects a
  # file this corpus does not host, and was sent in May 2025, so we can neither rotate it nor
  # meaningfully assess its life; (2) the source bytes are immutable public-records evidence and
  # altering them is barred by the chain-of-custody rule in the root CLAUDE.md; (3) this file is the
  # single most load-bearing message in the set — it is where Buck Canyon Properties, LLC and Derek
  # Harmon are joined on the federal record — so withholding it to suppress a stale password would
  # cost the site's clearest entity link.
  # The password is deliberately NOT reproduced in this file or in the collection's filename-map:
  # leaving it in the source is custody, quoting it into derived metadata is amplification.
  # ⚠️ It DOES remain in `data/site/passages.ndjson`, which indexes the published page's own text.
  # That is inherent to publishing the PDF — if the exposure is ever judged to outweigh the
  # evidence, the fix is a `withhold:` rule on this one rel plus `watermark passages`, NOT an edit.
  # review: { by: goedelsoup, date: 2026-08-16, result: publish, basis: public-agency-correspondence }
  - 'usace/west-union/*'
  # American Township's Board of Zoning Appeals file (#2171) — the Case #BZA 2024-12 conditional-
  # use packet: Conditional Use Permit No. 103, the BZA's final written decision, the hearing
  # worksheet and Order of Proceedings, the signed attendance sheet, an Article 17 excerpt, one
  # resident's stamped written comment, and the 2018 Churchill Farm -> Neff Farms warranty deed
  # bound in as a title attachment. The decision file of a noticed public hearing before a township
  # board, plus a recorded conveyance the Allen County recorder already publishes.
  #
  # ⚠️ A GLOB RATHER THAN THE `american-township` COLLECTION, AND THAT IS THE WHOLE POINT OF THE
  # RULE. The collection also holds `american-township/meetings/` — 49 trustee minutes that NO
  # review has covered, and which are known to print at least one resident's home address in the
  # clear ("Becky and Dave Streeter 1640 W State Rd", 12-9-24 minutes, granting privilege of the
  # floor). A collection rule would publish all 49 on this clearance. Screen that tree on its own
  # decision; this rule reaches `zoning/` only.
  #
  # ⚠️ SWEPT BY RENDERING EVERY PAGE, NOT BY READING TEXT. All 15 pages carry a ZERO-CHARACTER text
  # layer, so a grep or regex screen over this file matches nothing and reports clean while the
  # document carries a signed private letter and a handwritten address. Each page was rendered with
  # pypdfium2 and read as an image (150 DPI survey, 300-400 DPI on every block of small print and
  # every handwritten field); p. 14 is bound UPSIDE DOWN and scores blank unless rotated. Full
  # per-page result: data/extracted/american-township/zoning/pii-screen.yaml.
  # Found: no SSNs, no dates of birth, no account numbers, no residential address of a private
  # individual, and no personal telephone or email. The only email and direct-dial in the packet
  # are the applicant attorney's work details, printed on the face of the permit.
  #
  # ⚠️ SEVENTEEN PRIVATE INDIVIDUALS ARE NAMED, cleared knowingly. Fourteen residents are recorded
  # as testifying or corresponding at the two public hearings, one as supporting by email, and two
  # more sign the 2018 deed (the grantor's member and an Oklahoma notary). NONE is paired with an
  # address, telephone or email. Names of people who spoke at a noticed public hearing are what a
  # hearing record is made of. Two pages get a second look and are cleared on their own terms:
  # pp. 4-5 are Rebecca Streeter's SIGNED letter, stamped into the record by the township, which
  # exists to CORRECT the public account of her own testimony — withholding it would leave the
  # uncorrected version standing, and she is named as a testifier on pp. 1-2 regardless; p. 11
  # carries Neff Farms' handwritten tax mailing address on a deed the county serves from its own
  # public recorder portal, which is where these four pages were printed from.
  # ⚠️ A `withhold:` COULD NOT CARVE EITHER ONE OUT: withhold is per-file, and splitting a 15-page
  # source PDF is barred by the chain-of-custody rule in the root CLAUDE.md. The choice was the
  # whole packet or none of it, and it was made as the whole packet.
  #
  # ⚠️ PUBLISHING THIS DOES NOT MAKE IT FINDABLE. `extract_published_passages` returns early on a
  # page with no text layer, BEFORE its OCR fallback is reachable, so this document contributes
  # ZERO passages to search and /ask — downloadable and unsearchable, joining the known blind set
  # (790 of 5,531 published pages). Do not read a "the corpus does not mention X" negative as
  # covering it.
  # review: { by: goedelsoup, date: 2026-09-19, result: publish, basis: public-hearing-decision-file }
  - 'american-township/zoning/*'
documents:        # exact data/documents rels, e.g.  - recorder/bistrozzi-deeds/foo.pdf
  # The AEDG PRR-01 bundle — the full 328-page Port Authority public-records production.
  # Only pp. 317-327 (the six Tetra Tech OPC estimates) are the curated exhibit slice; the
  # rest is PAAC board minutes (pp. 1-90) and recorded real-estate instruments (pp. 91-295:
  # deeds, options, closings, conveyance-fee statements for the Brenneman/Miller/Neff/
  # Neighbors parcels). Published WHOLE, on the basis that it is a public-records production
  # already disclosed by PAAC and the instruments are county-recorder public record. It does
  # carry private parties' recorded signatures/addresses — accepted as disclosed public
  # record per the operator decision on #1301, not the outcome of a passed PII sweep.
  # review: { by: goedelsoup, date: 2026-07-08, result: publish, basis: disclosed-prr-production }
  - aedg/PRR-01-bundle.ocr.pdf
  # Ohio EPA Permit-to-Install P0138965 ("Bistrozzi LLC Data Center") — public agency record,
  # no private PII. Was previously public only via the sliced air-permit exhibit's source
  # auto-include (#1301 closed that path); kept public here as an explicit, reviewed entry.
  # review: { by: goedelsoup, date: 2026-07-08, result: publish, basis: public-epa-permit }
  - permits/bistrozzi-permits/4132514.pdf
  # The AEDG "Data Center Updates" web captures — AEDG's OWN public website pages about the
  # Google / Project BOSC data-center build (the marketing/news set captured through 2026-04-27;
  # provenance in data/documents/limaohio/SOURCES.md). Published on the basis that these are
  # already-public web pages by the Allen Economic Development Group with no private PII — the
  # captured bytes re-expose only what AEDG itself served publicly. Per the root CLAUDE.md,
  # any embedded analytics tokens are evidence, not a leak; the allowlist (not deletion) is the
  # exposure control. Listed per-document (not a whole-aedg-collection rule) so the PRR bundle's
  # recorded-instrument pages keep their own explicit basis and a future AEDG capture is reviewed.
  # review: { by: goedelsoup, date: 2026-07-15, result: publish, basis: aedg-public-web-captures }
  - aedg/data-center-updates/data-center-updates.index.html
  - aedg/data-center-updates/google-data-center-website-now-live-for-allen-county-community.html
  - aedg/data-center-updates/google-to-build-data-center-in-lima-ohio-with-500-million-capital-investment.html
  - aedg/data-center-updates/what-is-a-data-center-and-why-does-it-matter-for-communities-like-ours.html
  # The BOSC PRR productions produced TO the relator (Cory Parent) by Allen County and the Allen
  # Soil & Water Conservation District — the two June-12 batches. Published on the disclosed-PRR-
  # production basis (the same basis as aedg/PRR-01-bundle.ocr.pdf per #1301): these are public
  # agency records already disclosed by their producing agencies in response to an R.C. 149.43
  # request, and each producing agency did its OWN withholding/redaction before producing (the
  # County asserted no §9.66(D) withholding on the 06-12 batch; the SWCD withheld the plan sets
  # under §149.433 + §1333.61 and redacted the trade-secret plan-share links). They carry the
  # relator's own contact info plus third-party business contacts (engineers, contractors, county
  # officials) as disclosed public record — an operator decision to publish whole, NOT the outcome
  # of a passed C2 PII sweep (cf. the #1301 basis). Listed per-document so a future file added to
  # these production dirs is reviewed rather than auto-published; the sibling 06-05 and 07-24
  # batches stay unpublished until separately reviewed.
  # review: { by: goedelsoup, date: 2026-07-28, result: publish, basis: disclosed-prr-production }
  - "legal/prr-mandamus/prr-production-2026-06-12/Parent Records June 12.pdf"
  - "legal/prr-mandamus/prr-production-2026-06-12-aswcd/Mr. Parent Records request.pdf"
  - "legal/prr-mandamus/prr-production-2026-06-12-aswcd/Parent Records Request.pdf"

  # One Energy v. Allen Township, 2026-Ohio-405 — the published appellate decision in the
  # township zoning fight behind Findlay's governance record (#1265/#1463). A court's own
  # published opinion. An EXACT rel, not the `legal` collection: that tree is 2,383 files
  # spanning the select-committee record, the PRR mandamus production and the web-vendor
  # audit, none of which this review covers.
  # review: { by: goedelsoup, date: 2026-08-13, result: publish, basis: published-court-decision }
  - legal/one-energy-v-allen-twp/2026-Ohio-405.pdf

  # Three H.B. 646 witness submissions to the Ohio House Technology and Innovation committee /
  # Select Committee on Data Centers, May-June 2026 (#2047): Stephen E. Petty, P.E., C.I.H.,
  # C.S.P.; Leatra J. Harper for FreshWater Accountability Project Ohio; and Randi Pokladnik for
  # Ohio Valley Environmental Advocates. Testimony submitted to a legislative committee is
  # quintessentially public record, and the folder's existing witness submissions are published on
  # the same footing. Swept: full text layers, 40 pages, no personal data beyond each witness's own
  # name and organizational affiliation, which is the point of the document.
  #
  # EXACT rels, not the `legal` collection — that tree is thousands of files spanning the PRR
  # mandamus production and the web-vendor audit, which this review does not cover.
  # review: { by: goedelsoup, date: 2026-08-16, result: publish, basis: legislative-witness-testimony }
  - "legal/select-committee-2026/witnesses/Stephen Petty Data Center Committe Testimony.pdf"
  - "legal/select-committee-2026/witnesses/FreshWater Accountability Project Ohio Leatra J Harper.pdf"
  - "legal/select-committee-2026/witnesses/Ohio Valley Environmental Advocates-Randi Pokladnik.pdf"

# DECLARATIVE OPT-OUT — carve specific rels back out of the cleared scope above. A withhold
# rule WINS over everything, including the auto-included exhibits, so this is the authoritative
# way to keep a single file (a CBI page, a PII-bearing recorded instrument, a captured page
# that turned out to carry a live secret) off the public surface without un-clearing its whole
# boundary. Same three rule kinds as the cleared scope (mapping form), or a bare list of exact
# rels as shorthand. Record the reason with each entry, as with a clear.
#
# withhold:
#   documents:
#     - aedg/PRR-01-bundle.ocr.pdf   # example only — not withheld; PRR bundle is cleared whole
#   globs: []
#   collections: []
withhold:
  documents:
    # Hancock County's 2026 Directory of Officials. Cleared boundary (the `findlay`
    # collection), carved back out: 17 pages listing every county office and all 18 townships'
    # trustees and fiscal officers with what are plainly PERSONAL mobile numbers — the
    # township's own office line is printed separately on the same entry. That is ~200 named
    # individuals' personal contact details in one file, and the whole file is a finding aid:
    # nothing in the Findlay record rests on it, so publishing it buys no evidentiary value
    # for a real aggregation cost. The county publishes the PDF itself; re-serving it from a
    # searchable public site is a different act, and this is the file the C2 sweep flagged.
    # The corpus keeps it — withhold is exposure control, not removal, so the research agent
    # and the committed record are unaffected.
    # review: { by: goedelsoup, date: 2026-08-13, result: withhold, basis: aggregated-personal-contact-details }
    - "findlay/reference/2026 Directory of Officials_202604131412522342.pdf"
    # Van Wert City Council minutes of 2026-05-11. Cleared boundary (the `van-wert` collection),
    # carved back out on a CORRECTION to the record that clearance was approved on.
    #
    # The collection review above states that the meeting records carry names and signatures but
    # "no residential addresses, no phone numbers and no email addresses". That was established by
    # RENDERING SAMPLE PAGES — these PDFs have no text layer, so a PII regex returns a false clean —
    # and the sample missed page 7 of this file. Page 7 is a submitter roster with the literal
    # header "Name Address City State Zip" and FIFTEEN named private individuals' full residential
    # street addresses (1192 Nanette Ln, 121 E 2nd St, 1017 Maxine Ln, …), all Van Wert / Delphos.
    # It surfaced when the rebuilt passage index materialized it as searchable text.
    #
    # Nothing else in the corpus is close: a sweep for pages carrying 3+ street addresses beside an
    # Ohio ZIP returns 32 passages, and every other one is a facility address, an agency letterhead
    # or a recorded instrument in a disclosed PRR production. This page is the only ROSTER OF
    # PRIVATE RESIDENTS' HOME ADDRESSES in the published set, which is the Findlay
    # `aggregated-personal-contact-details` basis exactly — 25 people's contact details in one
    # place, aggregated by the document rather than incidental to it.
    #
    # ⚠️ This carve-out is NOT free, and the trade is deliberately visible. 2026-05-11 is the
    # sitting at which all three Mega Site ordinances passed — a primary instrument the assembly
    # and governance record cites. Withholding it costs the public download of that meeting record.
    # Withhold is exposure control, not removal: the bytes stay in the corpus, the extraction
    # (mega-site-instruments.yaml) is untouched, the record entry still renders, and the research
    # agent still reads it. Only the public download and the passage index lose it.
    #
    # Reversing this is a one-line delete plus `watermark passages` — but it re-publishes those 25
    # addresses, so it should be a decision taken on the corrected facts above rather than on the
    # collection review, which was written before page 7 was seen.
    # review: { by: goedelsoup, date: 2026-08-14, result: withhold, basis: aggregated-personal-contact-details }
    - van-wert/council/5.11.26.pdf
    # Ottawa Village "Verification of Lead Consumer Notice Issuance", eDoc docid 4251474,
    # published to the Ohio EPA portal 2026-08-27. Cleared boundary (the `oepa` collection's
    # standing auto-publish rule), carved back out on the screen that rule's own ⚠️ note demands
    # of every new oepa/<slug> pull.
    #
    # Page 3 is a completed Consumer Notice of Tap Water Lead Testing Result naming a PRIVATE
    # INDIVIDUAL and his home address in the same line, twice — once as the addressee and once as
    # "Location of sample: … (Resident/Business: …)" — beside that tap's lead result and the
    # 2026-08-13 collection date. That is a named person's residential address bound to a
    # health-related test result at their home. It is a smaller aggregation than the Findlay
    # directory or the Van Wert council roster and a sharper disclosure than either, because the
    # address is not incidental to the document — the document exists to report a result AT that
    # address.
    #
    # ⚠️ THE TEXT LAYER DOES NOT SHOW IT THE WAY YOU WOULD EXPECT. The form's completed header
    # fields are handwritten and extract as noise ("PWS Name: ~' fi'( ~, ~ /"), which reads like a
    # document with nothing legible in it. The notice on page 3 is rendered text and IS
    # extractable — so this file would have failed a "handwriting, therefore unreadable" dismissal
    # and passed a careless skim of page 1. It was caught by rendering every page, which is the
    # method the van-wert/council/5.11.26.pdf carve-out above established after a PII regex
    # returned a false clean there.
    #
    # Withhold is exposure control, not removal: the bytes stay in the corpus, the manifest entry
    # and its provenance are untouched, and the research agent still reads it. Only the public
    # download and the passage index lose it. Nothing in the Ottawa water record rests on this
    # document — it is cited in data/extracted/ottawa/water-watch.yaml only as evidence that the
    # lead consumer-notice process was in play, and that citation survives the withhold.
    #
    # The other addresses in the 2026-09-16 oepa pull were screened at the same time and are all
    # institutional: 515 E. Main St. (City of Van Wert), 347 N. Dunbridge Road (Ohio EPA NWDO),
    # 50 W. Town Street (Ohio EPA CO), 136 N Oak St. and 1972 S Agner St. (Ottawa village hall and
    # water plant), 256 Williamstown Rd (Putnam County Health Department), 1201 South River Rd and
    # 301 South Main Street (Findlay WPCF and city). Two Van Wert TTHM distribution sample points
    # (2115 N. Washington St., one of them expressly a hydrant, and 800 N. Washington St. in
    # edoc-4253933.pdf) carry NO NAME and are monitoring locations rather than lead premise
    # samples; they are recorded here as examined and left published, not overlooked.
    #
    # The carve-out was applied fail-safe by the agent that shelved the document, before review,
    # because publishing something it could not fully read would have been the irreversible
    # direction. The operator was shown the finding and directed the withhold to stand.
    # Reversing it is a one-line delete plus `watermark passages` — but that re-publishes the
    # individual's name, home address, tap lead result and collection date as a searchable
    # passage, so it should be a decision taken on the facts above.
    # review: { by: goedelsoup, date: 2026-09-16, result: withhold, basis: personal-contact-details }
    # ⚠️ THE REL IS COLLECTION-PREFIXED. Written first as `ottawa/edoc-4251474.pdf`, which matched
    # NOTHING and published the document anyway — a withhold rule that does not match fails OPEN
    # and looks identical to one that works. The rel is the path under data/documents/, so the
    # `oepa` collection segment is part of it. Verify a new carve-out by grepping
    # data/site/passages.ndjson after `watermark passages`, never by reading the rule.
    - oepa/ottawa/edoc-4251474.pdf
