---
kind: home
h1: "A data center is coming to Fort Wayne"
kicker: "The story · Project Zodiac"
pageTitle: "Project Zodiac — the story"
description: "The Project Zodiac story: told through the records, with every figure checkable. (Scaffold — chapters in draft.)"
---

{/*
  SCAFFOLD (#741) — the Fort Wayne on-ramp, modeled on Lima's
  `stories/lima/project-bosc/_home.mdx`. This file is schema-valid and lives in the `stories`
  collection, but it does NOT render yet: the story routes are gated to `selectable` sites
  (`getStaticPaths` in `network/[site]/stories/[codename]/index.astro`), and Fort Wayne is not
  selectable. Bodies here are intentionally import-free so the collection always compiles.

  To bring it live, follow the checklist in `docs/onboarding.md` → "Bringing a site's story live".
  Replace this placeholder prose with the real on-ramp (the Lima `_home.mdx` re-adds the
  bundle-count imports + the `<div class="walk-promise">` blocks); write each chapter's body;
  then flip Fort Wayne to `selectable` once the parity gate (#746/#742) is met.
*/}

<p class="walk-onramp-lead">
  A hyperscale data-center campus — codename <strong>Project Zodiac</strong> — is being
  assembled in Fort Wayne, Indiana: an 11-parcel ownership block held by a Delaware-style
  shell, <strong>Hatchworks LLC</strong>, in the Maumee headwaters. This story will show what
  the public record reveals, and teach you how to read it yourself.
</p>

<p>
  <em>Scaffold — the chapters below are in draft (<code>live: false</code>). Each is anchored
  to a real committed Fort Wayne record; the teaching prose is still to be written.</em>
</p>
